<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-3">
                <%- include('./sidebar') %>
            </div>
            <div class="col-lg-8">
                <div class="card shadow-lg rounded-4">
                    <div class="card-header text-white text-center rounded-top-4">
                        <h4 class="my-1">Edit Book Details</h4>
                    </div>
                    <div class="card-body">
                        <form action="/updateBook/<%= singleRecord.id %>" method="post" enctype="multipart/form-data">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Title:-</label>
                                    <input type="text" name="title" value="<%= singleRecord.title %>"
                                        class="form-control" placeholder="Enter book title...">
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Author:-</label>
                                    <input type="text" name="author" value="<%= singleRecord.author %>"
                                        class="form-control" placeholder="Enter author name...">
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Description:-</label>
                                    <textarea class="form-control" name="description" rows="3"
                                        placeholder="Enter description..."><%= singleRecord.description %></textarea>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Genre:-</label><br>
                                    <div class="form-check form-check-inline custom-check">
                                        <input class="form-check-input" name="genre" value="Fiction" type="checkbox"
                                            id="fiction" <% if(singleRecord.genre.includes('Fiction')){%>
                                        <%= 'checked' %>
                                            <% } %> >
                                                <label class="form-check-label" for="fiction">Fiction</label>
                                    </div>
                                    <div class="form-check form-check-inline custom-check">
                                        <input class="form-check-input" name="genre" value="Non-fiction" type="checkbox"
                                            id="nonfiction" <% if(singleRecord.genre.includes('Non-fiction')){%>
                                        <%= 'checked' %>
                                            <%}%>>
                                                <label class="form-check-label" for="nonfiction">Non-Fiction</label>
                                    </div>
                                    <div class="form-check form-check-inline custom-check">
                                        <input class="form-check-input" name="genre" value="Mystery" type="checkbox"
                                            id="mystery" <% if(singleRecord.genre.includes('Mystery')){ %>
                                        <%= 'checked' %>
                                            <%}%>>
                                                <label class="form-check-label" for="mystery">Mystery</label>
                                    </div>
                                    <div class="form-check form-check-inline custom-check">
                                        <input class="form-check-input" name="genre" value="Romance" type="checkbox"
                                            id="Romance" <% if(singleRecord.genre.includes('Romance')){ %>
                                        <%= 'checked' %>
                                            <%}%>>
                                                <label class="form-check-label" for="Romance">Romance</label>
                                    </div>
                                    <div class="form-check form-check-inline custom-check">
                                        <input class="form-check-input" name="genre" value="Fantasy" type="checkbox"
                                            id="fantasy" <% if(singleRecord.genre.includes('Fantasy')){ %>
                                        <%= 'checked' %>
                                            <%}%>>
                                                <label class="form-check-label" for="fantasy">Fantasy</label>
                                    </div>
                                    <div class="form-check form-check-inline custom-check">
                                        <input class="form-check-input" name="genre" value="Horror" type="checkbox"
                                            id="Horror" <% if(singleRecord.genre.includes('Horror')){ %>
                                        <%= 'checked' %>
                                            <%}%>>
                                                <label class="form-check-label" for="Horror">Horror</label>
                                    </div>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">Total:-</label>
                                    <input type="number" name="total" value="<%= singleRecord.total %>"
                                        class="form-control" placeholder="Enter total number...">
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label mb-0">Image:-</label>
                                    <div class="d-flex align-items-center">
                                        <input class="form-control w-75 me-2" value="<%= singleRecord.image %>"
                                            name="image" type="file">
                                        <img src="<%= singleRecord.image %>" class="img-fluid rounded" width="90">
                                    </div>

                                </div>
                            </div>

                            <div class="text-center mt-3">
                                <button type="submit" class="btn text-white rounded-pill px-4 py-2 custom-btn">
                                    Update Book</button>
                            </div>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>