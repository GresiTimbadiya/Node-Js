<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css">
</head>

<body>
    <div align="center">
        <h2 class="mt-3">View Todo</h2>
        <table class="table table-bordered table-striped table-hover w-75 mx-auto mt-4 shadow">
            <thead class="table-dark text-center">
                <tr>
                    <th>Id</th>
                    <th>Task</th>
                    <th>Status</th>
                    <th>Priority</th>
                    <th>Due Date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody class="text-center">
                <% taskList.map((val)=> { %>
                    <tr>
                        <td>
                            <%= val.id %>
                        </td>
                        <td>
                            <%= val.task %>
                        </td>
                        <td>
                            <%= val.status %>
                        </td>
                        <td>
                            <%= val.priority %>
                        </td>
                        <td>
                            <%= val.date %>
                        </td>
                        <td>
                            <a href="/deleteTodo?id=<%=val.id%>" class="btn btn-sm btn-danger me-1">Delete</a>
                            <a href="/editTodo?id=<%=val.id%>" class="btn btn-sm btn-warning">Edit</a>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
        <a href="/addTodo" class="btn btn-success">Add Task +</a>
    </div>
</body>

</html>